# Devlog — 2026-03-02

## 今日完成

### T-87 Bugfix: 论文搜索 5 个代码审查 bug 修复
- SearchResultCard 补全 crossref 来源颜色/标签映射
- CrossRef 日期解析增加 None/非 int 安全检查
- Agent 深读 prompt 摘要分隔符添加换行
- paper-search / smart-search CLI 命令 try/finally 防止 httpx Client 泄漏

### T-87 Bugfix: 论文搜索结果切换页面后消失
- 根因：PaperSearchPage 所有状态在组件本地 useState，路由切换时组件卸载，状态丢失
- Smart Search 更严重：useMutation 数据不被 React Query 缓存管理
- 修复：模块级缓存 pageCache 保存页面状态，组件卸载时写入、挂载时恢复
- Smart Search 数据用额外 useState + useEffect 同步 mutation 结果

### T-87 Bugfix 验证补全：测试 + webapp-testing
- 修复已有测试 `test_all_five_providers_registered` → `test_all_providers_registered`（6 个 provider）
- 新增 TestCrossRefProvider（8 个测试）：正常解析、JATS 清理、作者截断、URL 回退、year 边界 4 case、错误处理
- 新增 TestCLIResourceCleanup（2 个测试）：paper-search / smart-search 异常后 sm.close() 仍被调用
- 全量 pytest 198 passed，vitest 30 passed
- Playwright MCP webapp-testing 验证：
  - 搜索 "deep learning" → 10 条结果（5 PubMed + 5 CrossRef）
  - 切到仪表盘 → 切回论文搜索 → 结果完整保留 ✓
  - CrossRef badge 正确显示 ✓

## 遇到的问题

- React Router 路由切换导致子页面组件完全卸载，useState 全部重置。项目无 zustand，用模块级变量作为轻量缓存方案。
- webapp-testing 用 Python Playwright 脚本无法成功触发搜索（`fill()` + `click()` 不触发 React handleSearch），改用 MCP Playwright 工具 + Enter 键成功。
- Python Playwright 脚本等待时间不足导致外部 API 结果未返回，MCP Playwright 手动等 30s 后成功获得结果。

## 经验总结

- WORKFLOW-bugfix Phase 2（写测试）不可跳过，Phase 4 验证必须包含 pytest + vitest + webapp-testing
- docker build 成功 ≠ 验证通过
- webapp-testing 优先用 MCP Playwright 工具（交互式、可视化），Python 脚本作为备选

### 论文搜索三项改进（并行 Agent 实现）

**Task 1 — 修复「分析并存入知识库」**
- 根因：`_try_download_pdf()` 对非 PDF URL 返回 None → `store_metadata_only()` → 详情页空白
- 修复：新增 `_analyze_from_metadata()` — LLM 从标题+摘要生成结构化 AnalysisResult
- `save_to_kb` 和 `_do_download_and_analyze` 均改为优先 `store_analysis()`
- 最终兜底仍保留 `store_metadata_only`（LLM 也失败时）

**Task 2 — 知识库详情页「论文原文」Tab**
- `knowledge_base.py`: 新增 `parsed_text` 列迁移 + `store_analysis()` 增加参数
- `engine.py` / `pipeline_runner.py` / `research.py`: 所有 `store_analysis()` 调用传 `parsed_text=doc.full_text`
- `knowledge.py` API: SELECT + 返回值增加 `parsed_text`
- `DocumentDetailPage.tsx`: Tab 状态扩展为 `'cards' | 'report' | 'original'`

**Task 3 — 论文搜索 AI 对话**
- 后端：`POST /chat` SSE 端点，queue + threading 模式
- 前端：`PaperChatDialog.tsx` 新建，`streamPaperChat()` SSE 客户端
- `SearchResultCard` 新增 `onChat` prop + AI 对话按钮

**执行方式：3 个 worktree 隔离 Agent 并行 → 手动合并**
- Agent-2 patch 最先合入（无冲突）
- Agent-1/3 共享文件（paper_search.py, schemas.py, i18n）手动合并
- 修复合并后 TypeScript 编译错误（SaveToKBResponse 类型、useSaveToKB hook 缺失）
- 验证：ruff check ✓ / tsc --noEmit ✓ / pytest 198 passed ✓
- 提交：`feat: 论文搜索三项改进 — 分析修复 + 原文 Tab + 论文对话`（16 files, +612/-150）

## 遇到的问题

- React Router 路由切换导致子页面组件完全卸载，useState 全部重置。项目无 zustand，用模块级变量作为轻量缓存方案。
- webapp-testing 用 Python Playwright 脚本无法成功触发搜索（`fill()` + `click()` 不触发 React handleSearch），改用 MCP Playwright 工具 + Enter 键成功。
- Python Playwright 脚本等待时间不足导致外部 API 结果未返回，MCP Playwright 手动等 30s 后成功获得结果。
- **Worktree 并行合并冲突**：3 个 agent 共享 `paper_search.py` 和 `schemas.py`，git apply 无法直接打补丁，需手动逐文件合并。
- **Agent 遗漏类型/hook**：Agent-3 在 PaperSearchPage 中使用了 `useSaveToKB` 和 `SaveToKBResponse`，但未创建对应定义，合并后 tsc 报错需手动补全。

## 经验总结

- WORKFLOW-bugfix Phase 2（写测试）不可跳过，Phase 4 验证必须包含 pytest + vitest + webapp-testing
- docker build 成功 ≠ 验证通过
- webapp-testing 优先用 MCP Playwright 工具（交互式、可视化），Python 脚本作为备选
- **并行 Agent worktree 合并时共享文件必须手动 merge**，git apply 对上下文敏感容易失败
- **Agent 完成后必须跑 tsc 验证**，子 agent 在隔离 worktree 中不会检测到跨文件依赖缺失

## 明日计划

- Docker 构建部署验证
- 手动测试三项新功能（分析入库/原文 Tab/AI 对话）
