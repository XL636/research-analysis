# 2026-02-21 开发日志

## 今日完成

### Phase 5: 实用功能 + Docker 容器化

**PR #1 合并**
- 合并 `feature/research-analysis-system` → `master`（Phase 0-4, 34 tasks, 168 tests）
- 创建新分支 `feature/phase5-docker`

**T-35: 批量处理 CLI**
- 新增 `batch` 命令，扫描目录批量分析文档
- 支持 .pdf/.pptx/.md/.txt/.docx 格式
- `--recursive` 递归子目录，`--output-dir` 自定义输出，`--synthesize` 综合分析
- Rich 进度条 + 结果汇总表

**T-36: 知识库导出/导入**
- KnowledgeBase 新增 `export_json`、`export_csv`、`import_json` 方法
- CLI 新增 `export` (JSON/CSV) 和 `import` (JSON) 命令
- 完整的标签、分析结果 roundtrip 保持

**T-37: 模型调用成本统计**
- 新增 `UsageStats`/`ModelUsage` Pydantic 模型
- `LLMClient.chat()` 自动从 `response.usage` 记录 token 数
- Pipeline 运行后打印按模型分组的 Rich 表格成本摘要
- `PipelineContext.usage_stats` 暴露统计数据

**T-38: Docker 容器化**
- 多阶段 Dockerfile: `node:22-alpine` 构建前端 + `python:3.11-slim` 运行时
- `docker-compose.yml`: 配置卷挂载、健康检查、端口映射
- `.dockerignore`: 优化构建上下文

### 测试统计
- Python: 166 tests (原 138 + 28 新增)
- Frontend: 30 Vitest tests
- 合计: 196 tests，全部通过

## 遇到的问题

1. **GitHub API token 权限不足**：无法通过 MCP 合并 PR，改用本地 `git merge`
2. **Engine 测试 mock 兼容**：新增 `usage_stats` 同步时，mock 的 `LLMClient` 返回 MagicMock 导致类型错误，通过 `isinstance` 检查解决

## 后续方向

- [ ] 音频转录 Agent（faster-whisper）
- [ ] 异步并行处理多文档
- [ ] 自定义报告模板系统
- [ ] 会议报告专用模板
