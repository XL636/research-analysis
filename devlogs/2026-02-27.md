# Devlog — 2026-02-27

## 今日完成

### T-79: 阅读助手流式输出 + 智能上下文

**流式输出（Part A）：**
- `LLMClient.stream_chat()` — 基于 OpenAI SDK `stream=True`，逐块 yield 文本
- SSE 端点 `POST /stream-chat` — 事件格式 delta/done/error，线程+队列桥接同步流式到 async
- 前端 `streamSessionChat()` — fetch + ReadableStream 解析 POST SSE
- `useStreamChat()` hook — 管理 streamingContent/isStreaming/error 状态
- ChatPanel 流式渲染 — Markdown 实时渲染 + 闪烁光标 `▌`，等待首字时保留跳动圆点
- 重构 `_prepare_chat_context()` — 验证/上下文构建/保存用户消息共享逻辑，旧端点兼容

**智能上下文（Part B）：**
- `reader_pages_fts` FTS5 虚拟表 — contentless 模式，启动时自动回填已有数据
- `search_pages()` / `get_pages_by_nums()` 新方法
- 三策略选页路由：fixed（±N页）/ keyword（FTS5）/ smart（FTS粗筛+LLM精排）
- 配置 `context_strategy: smart` + `context_search_limit: 5`

**改动文件（11个）：**
- 后端：llm_client.py, reader_store.py, routes/reader.py
- 前端：reader.ts, useReader.ts, ChatPanel.tsx, ReaderViewPage.tsx
- 配置：settings.yaml
- 追踪：TASKS.md, PROGRESS.md, CHANGELOG.md

### Bugfix: PDF 加载 + 删除失败

- 修复 PDF worker 加载：`new URL()` 改用 Vite `?url` 导入，新增 `vite-env.d.ts` 类型声明
- PdfPageViewer 加载前 HEAD 预检文件，404 时提示"文件不存在，请重新上传"（根因：Docker 命名卷 reader 文件丢失）
- 修复删除文档 500 错误：FTS5 索引损坏导致 `database disk image is malformed`，改为 try/except + 自动 rebuild/重建 FTS 表

## 技术决策
- FTS5 用 contentless 模式（`content='reader_pages'`）避免数据重复存储
- SSE 用 queue + threading 桥接同步 OpenAI stream 到 FastAPI async generator
- smart 策略精排复用 glm-4-flash（推荐问题同款），成本极低
- 旧 `/chat` 端点保留不动，向后兼容

## 明日计划
- 观察 smart 策略实际效果，必要时调优 FTS 候选数和精排 prompt
