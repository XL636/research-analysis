# Devlog — 2026-02-22

## 今日完成

### T-42: Per-Agent 模型选择 + 内联 API Key 配置
- 后端新增 5 个 Pydantic schemas (`ModelInfo`, `AgentModelAssignment` 等)
- 后端新增 `GET/PUT /api/settings/agent-models` 端点，支持查询和更新 Agent-模型分配
- 新增 `_load_full_config()` / `_save_config()` 辅助函数，读写完整 settings.yaml
- 前端新增 `AgentModelCard` 组件：模型下拉框 + 内联 API Key 输入
- 重构 `SettingsPage`：Agent 模型卡片网格（主区域）+ 可折叠 API Key 表格（次要区域）
- i18n 新增 ~15 个双语翻译键（agent 名称/描述、状态标签等）
- 所有 166 Python + 30 前端测试通过

## 注意事项
- `_save_config()` 使用 `yaml.dump()` 写回会丢失原始 YAML 注释，这是已知的 trade-off
- 模型下拉切换后即时 PUT 保存，无需额外的"保存"按钮

## 明日计划
- 待定
