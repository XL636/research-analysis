# Devlog — 2026-02-25

## 今日完成

### 安全漏洞与代码质量修复（4 commits）

1. **fix(security): 路径遍历 + Docker root 运行**
   - SPA fallback: `resolve()` + `is_relative_to()` 阻止目录遍历
   - 文件上传: `Path.name` 清理文件名 + 50MB 大小限制
   - Dockerfile: 添加 `appuser` 非 root 用户 + `UV_CACHE_DIR`

2. **fix(quality): CORS、HTML 注入、临时文件泄露、裸 except**
   - CORS: `*` 改为显式白名单
   - PDF 输出: `html.escape()` 防注入
   - 报告下载: 异常时清理 tmp_dir
   - 11 处 `except: pass` → `logger.debug/warning`

3. **chore: .gitignore 追加密钥规则**

4. **docs: CHANGELOG 更新**

### Docker volume 权限修复（1 commit）

5. **fix(security): Docker volume 权限问题**
   - 根因：named volumes 首次挂载以 root 创建，appuser 无写权限 → 上传/KB/输出全部 PermissionError → 前端按钮无响应
   - 修复：新增 `docker-entrypoint.sh`，启动时 root 修复权限，`gosu` 降权到 appuser 运行

## 遇到的问题

1. `uv` 缓存目录权限错误（`/home/appuser/.cache/uv` 不存在）→ `ENV UV_CACHE_DIR=/app/.uv-cache`
2. Docker named volumes 以 root 创建覆盖镜像内 chown → 添加 entrypoint + gosu 方案

## 验证结果

- `curl ../../config/.env` → 返回 index.html（路径遍历已阻止）
- URL 编码 / 双重编码 / assets 前缀遍历均被拦截
- 容器主进程 UID=1000（appuser）
- uploads/kb/output 目录均可写
- 仪表盘、分析页、知识库页正常加载和交互
