# Devlog — 2026-03-03

## 今日完成

### Smart Search 三项 Bug 修复

1. **中文查询 fallback 翻译**
   - 问题：ZHIPU_API_KEY 未设置时，LLM 调用失败，中文原文直接发给英文学术 API，搜到完全不相关的结果
   - 修复：新增 `_fallback_translate_and_detect()` — 80+ 学术术语词典（中文→英文），覆盖经济/金融/管理/AI/医学等领域
   - 采用非消耗式扫描匹配，允许重叠（如"资本市场"+"市场反应"同时命中）

2. **LLM 排序分数解析崩溃**
   - 问题：`_filter_and_rank()` 中 `float(item.get("relevance_score"))` 崩溃——LLM 返回了文字描述而非数字
   - 修复：try/except 安全解析，失败时默认 0.5 分

3. **前端超时导致"未找到论文"**
   - 问题：Agent 4 轮迭代 + 最终 LLM 排序耗时 ~3.5 分钟，超过前端 120s 超时
   - 修复：迭代上限 4→3，内部超时 100→60s，前端超时 120→180s

### 扩展领域映射

`_DOMAIN_PROVIDERS` 新增 10 个领域：economics/finance/business/management/social_science/psychology/education/law/environmental/engineering

### 无 LLM Fallback 排序

新增 `_fallback_rank()` 方法：基于关键词匹配的相关性评分，替代 LLM 不可用时的全部 0.5 默认分

## 验证

- 32 个 paper_search 测试全部通过
- Docker build 成功
- Web UI 端到端验证：搜索"代理经济与资本市场反应"返回 Jensen & Meckling 1976、Eisenhardt 1989 等经典代理理论论文
- API curl 测试：154 篇候选，10 篇高相关结果

## 遇到的问题

- Semantic Scholar 无 API Key 频繁 429 限流 + circuit breaker 触发
- 前端超时问题不易发现——后端返回 200 OK 但前端已断开连接

## 明日计划

- 考虑申请 Semantic Scholar API Key 提升搜索覆盖
- 可考虑 smart-search SSE 流式返回（搜索进度实时推送到前端）
